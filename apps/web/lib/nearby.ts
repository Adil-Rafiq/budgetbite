/**
 * Radius used for the "restaurants near you" proof, wherever a location is
 * being picked or changed. Matches the default on the restaurants browse page
 * so the count shown next to the map is the same one the user will see when
 * they go browsing.
 */
export const NEARBY_PROOF_RADIUS_KM = 10;
